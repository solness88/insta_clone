<h1>ブログ一覧</h1>
<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>
<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= post.content %></td>
    <td><%= link_to '詳細を確認する', post_path(post.id) %></td>
    <td><%= link_to '投稿編集画面へ', edit_post_path(post.id) %></td>
  </tr>
<% end %>
</table>
<div><%= link_to '投稿一覧画面に戻る', posts_path %></div>
<div><%= link_to '新規作成画面へ', new_post_path %></div>
